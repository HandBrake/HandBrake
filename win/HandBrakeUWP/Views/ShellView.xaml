<Page
    x:Class="HandBrake.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HandBrake.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:cal="using:Caliburn.Micro"
    mc:Ignorable="d"
    AllowDrop="True"
    x:Name="shellView">

    <interactivity:Interaction.Behaviors>
        <Core:EventTriggerBehavior EventName="DragEnter">
            <cal:ActionMessage MethodName="DragEntered">
                <cal:Parameter Value="$eventArgs" />
            </cal:ActionMessage>
        </Core:EventTriggerBehavior>

        <Core:EventTriggerBehavior EventName="Drop">
            <cal:ActionMessage MethodName="DataDropped">
                <cal:Parameter Value="$eventArgs" />
            </cal:ActionMessage>
        </Core:EventTriggerBehavior>
    </interactivity:Interaction.Behaviors>

    <Grid Background="{ThemeResource SystemControlAcrylicWindowBrush}">
        <ContentControl x:Name="MainViewModel"  IsEnabled="{Binding IsMainPanelEnabled}" />
    </Grid>
</Page>