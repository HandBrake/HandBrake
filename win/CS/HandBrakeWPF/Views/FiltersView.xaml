<UserControl x:Class="HandBrakeWPF.Views.FiltersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Converters="clr-namespace:HandBrakeWPF.Converters"
             xmlns:Properties="clr-namespace:HandBrakeWPF.Properties"
             xmlns:dd="urn:gong-wpf-dragdrop"
             mc:Ignorable="d" >

    <UserControl.Resources>
        <Converters:BooleanToVisibilityConverter x:Key="boolToVisConverter" />
        <Converters:EnumComboConverter x:Key="boolComboConverter" />
        <Converters:ThemeImageConverter x:Key="themeConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Row 0 -->
        <Grid Grid.Row="0" Margin="10,5,10,0" Grid.Column="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button x:Name="filterOptionsButton" FontWeight="Bold" Margin="0,0,10,0" Grid.Column="0" Click="OptionsButton_OnClick"
                        AutomationProperties.Name="{x:Static Properties:Resources.FilterView_AddFilter}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="0,0,5,0" Padding="0"
                                       VerticalAlignment="Center"
                                       Text="{x:Static Properties:Resources.FilterView_AddFilter}" />

                            <Path Height="5" Margin="2,2,0,0" VerticalAlignment="Center" HorizontalAlignment="Center"
                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                  Fill="{DynamicResource ButtonGlyphBrush}" x:Name="dropdownArrowPreset" />
                        </StackPanel>
                    </Button.Content>

                    <Button.ContextMenu>
                        <ContextMenu x:Name="MainContextMenu" PlacementRectangle="{Binding RelativeSource={RelativeSource Self}}" ItemsSource="{Binding AvailableFiltersMenu}">
                            <ContextMenu.ItemContainerStyle>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Header" Value="{Binding Header}" />
                                    <Setter Property="ItemsSource" Value="{Binding Children}" />
                                    <Setter Property="Command" Value="{Binding DataContext.RelayCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <Setter Property="CommandParameter" Value="{Binding Filter}" />
                                </Style>
                            </ContextMenu.ItemContainerStyle>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>

                <Button MinWidth="65" Grid.Column="4"
                        Margin="0,0,0,0"
                        Command="{Binding RelayCommand}" CommandParameter="Clear"
                        Content="{x:Static Properties:Resources.Generic_Clear}"
                        AutomationProperties.Name="{x:Static Properties:Resources.Generic_Clear}" />
            </Grid>
        </Grid>

        <!-- Row 1 -->
        <ListView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" VerticalAlignment="Stretch"
                  Margin="10,10,10,10"
                  dd:DragDrop.DropHandler="{Binding}"
                  dd:DragDrop.IsDragSource="True"
                  dd:DragDrop.IsDropTarget="True"
                  Background="Transparent"
                  ItemsSource="{Binding VideoFilters}"
                  BorderBrush="{StaticResource Ui.ContrastLight}"
                  SelectionMode="Extended">

            <ListView.Resources>
                <Style TargetType="{x:Type GridViewColumnHeader}" >
                    <Setter Property="Template" >
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                <Border BorderBrush="{StaticResource Ui.ContrastLight}" BorderThickness="0,0,0,1" Margin="0,0,0,5">
                                    <TextBlock Text="{TemplateBinding Content}" TextAlignment="Left" Margin="0,2,0,3" Style="{StaticResource sectionHeader}"
                                               TextTrimming="CharacterEllipsis"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.Resources>

            <ListView.ContextMenu>
                <ContextMenu>

                    <MenuItem Header="{x:Static Properties:Resources.FilterView_AddFilter}" ItemsSource="{Binding AvailableFiltersMenu}">
                        <MenuItem.ItemContainerStyle>
                            <Style TargetType="MenuItem">
                                <Setter Property="Header" Value="{Binding Header}" />
                                <Setter Property="ItemsSource" Value="{Binding Children}" />
                                <Setter Property="Command" Value="{Binding DataContext.RelayCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                <Setter Property="CommandParameter" Value="{Binding Filter}" />
                            </Style>
                        </MenuItem.ItemContainerStyle>
                    </MenuItem>
                    <Separator/>
                    <MenuItem Header="{x:Static Properties:Resources.QueueView_Delete}" Command="{Binding DeleteCommand}"
               CommandParameter="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                    <Separator/>
                    <MenuItem Header="{x:Static Properties:Resources.Generic_Clear}"  Command="{Binding RelayCommand}" CommandParameter="Clear" />
                </ContextMenu>
            </ListView.ContextMenu>

            <ListView.View>
                <GridView AllowsColumnReorder="False">
                    <GridViewColumn Header="" Width="28" />
                    <GridViewColumn Header="{x:Static Properties:Resources.FilterView_Filter}" Width="150" />
                    <GridViewColumn Header="{x:Static Properties:Resources.FilterView_Preset}" Width="150" />
                    <GridViewColumn Header="{x:Static Properties:Resources.FilterView_Tune}" Width="150" />
                    <GridViewColumn Header="{x:Static Properties:Resources.FilterView_Custom}" Width="400" />
                </GridView>
            </ListView.View>

            <ListView.InputBindings>
                <KeyBinding Key="Delete" Command="{Binding DeleteCommand}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListBox}}}"  />
            </ListView.InputBindings>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Margin" Value="0,0,0,1" />
                    <EventSetter Event="PreviewMouseRightButtonDown" Handler="ListViewItem_PreviewMouseRightButtonDown" />
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>

                    <Grid HorizontalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!--  Settings  -->
                        <Grid Grid.Column="1" Grid.Row="0" HorizontalAlignment="Stretch" Margin="0,5,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="150"/> <!-- Filter -->
                                <ColumnDefinition Width="150" /> <!-- Preset -->
                                <ColumnDefinition Width="150" /> <!-- Tune -->
                                <ColumnDefinition Width="400" /> <!-- Custom -->
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="28" />
                            </Grid.RowDefinitions>

                            <Image Width="16" Source="{Binding Converter={StaticResource themeConverter}, ConverterParameter='Reorder.png'}" />

                            <!--  Row 1  -->
                            <TextBlock Text="{Binding DisplayName}" Grid.Column="1" Margin="5,0,0,0" />

                            <ComboBox Grid.Column="2"  Margin="5,0,5,0" HorizontalAlignment="Stretch" ToolTip="Presets for this filter." DisplayMemberPath="DisplayName"
                                      ItemsSource="{Binding AvailablePresets}" SelectedItem="{Binding Preset}" Visibility="{Binding CanSetPreset, Converter={StaticResource boolToVisConverter}}"
                                      AutomationProperties.Name="{x:Static Properties:Resources.FilterView_Preset}" />

                            <ComboBox Grid.Column="3"  Margin="5,0,5,0" HorizontalAlignment="Stretch" ToolTip="Tunes for this filter." DisplayMemberPath="DisplayName"
                                      ItemsSource="{Binding AvailableTunes}" SelectedItem="{Binding Tune}" Visibility="{Binding CanSetTune, Converter={StaticResource boolToVisConverter}}"
                                      AutomationProperties.Name="{x:Static Properties:Resources.FilterView_Tune}" />

                            <TextBox Text="{Binding CustomOptions}" Grid.Column="4" Margin="5,0,5,0" HorizontalAlignment="Stretch" ToolTip="Custom options for this filter."
                                     Visibility="{Binding AllowsCustomOptions, Converter={StaticResource boolToVisConverter}}"
                                     AutomationProperties.Name="{x:Static Properties:Resources.FilterView_Custom}" />
                        </Grid>

                        <!--  Delete  -->
                        <StackPanel Grid.Column="2" Grid.Row="0" VerticalAlignment="Center">

                            <Button Style="{StaticResource TransparentButtonStyle}" Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}"          
                                    CommandParameter="{Binding}"
                                    AutomationProperties.Name="{x:Static Properties:Resources.QueueView_Delete}">
                                <Button.Content>
                                    <Image Width="16" Height="16" Margin="10,0,10,0" Source="Images/close64.png" />
                                </Button.Content>
                            </Button>
                        </StackPanel>                                                                        
                    </Grid>

                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>

</UserControl>
