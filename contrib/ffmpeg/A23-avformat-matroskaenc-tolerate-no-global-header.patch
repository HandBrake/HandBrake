--- a/libavformat/matroskaenc.c	2025-11-20 03:31:18.000000000 +0100
+++ b/libavformat/matroskaenc.c	2025-12-03 20:05:52.669560446 +0100
@@ -1137,11 +1137,25 @@
     case AV_CODEC_ID_WAVPACK:
         return put_wv_codecpriv(dyn_cp, extradata, extradata_size);
     case AV_CODEC_ID_H264:
-        return ff_isom_write_avcc(dyn_cp, extradata,
-                                  extradata_size);
+        if (extradata_size) {
+            av_log(s, AV_LOG_VERBOSE, "mkv_assemble: h264: extradata_size %d\n", extradata_size);
+            return ff_isom_write_avcc(dyn_cp, extradata, extradata_size);
+        } else {
+            av_log(s, AV_LOG_WARNING, "mkv_assemble: h264: zero extradata\n");
+            *size_to_reserve = 0; // allow missing global header, see https://trac.ffmpeg.org/ticket/10667
+        }
+        break;
+
     case AV_CODEC_ID_HEVC:
-        return ff_isom_write_hvcc(dyn_cp, extradata,
-                                  extradata_size, 0, s);
+        if (extradata_size) {
+            av_log(s, AV_LOG_VERBOSE, "mkv_assemble: hevc: extradata_size %d\n", extradata_size);
+            return ff_isom_write_hvcc(dyn_cp, extradata, extradata_size, 0, s);
+        } else {
+            av_log(s, AV_LOG_WARNING, "mkv_assemble: hevc: zero extradata\n");
+            *size_to_reserve = 0; // allow missing global header, see https://trac.ffmpeg.org/ticket/10667
+        }
+        break;
+
     case AV_CODEC_ID_VVC:
         return ff_isom_write_vvcc(dyn_cp, extradata,
                                   extradata_size, 0);
